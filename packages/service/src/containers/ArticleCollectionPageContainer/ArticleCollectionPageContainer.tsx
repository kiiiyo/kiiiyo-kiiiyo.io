import { FC } from 'react'
//
import { Pages } from '../../components'

export const ArticleCollectionPageContainer: FC = (props) => {
  return <Pages.ArticleCollectionPage />
}

export default ArticleCollectionPageContainer
