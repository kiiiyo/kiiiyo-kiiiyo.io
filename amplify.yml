version: 1
applications:
  - frontend:
    phases:
      preBuild:
        commands:
          - 'nvm use "${VERSION_NODE_12}"'
          - 'yarn install'
      build:
        commands:
          - npm run mc:build
    artifacts:
      baseDirectory: /
      files:
        - '**/*'
    cache:
      paths: []
    appRoot: packages/service
